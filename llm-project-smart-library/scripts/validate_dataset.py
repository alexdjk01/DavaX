"""Validează/dedup-ează JSON-ul generat, verifică schema și lungimile rezumatelor."""
import json
from pathlib import Path

DATA_DIR = Path(__file__).resolve().parents[1] / "backend" / "data"

def main():
    print("[TODO] validate_dataset: verifică lipsuri, duplicări, lungimi…")

if __name__ == "__main__":
    main()
